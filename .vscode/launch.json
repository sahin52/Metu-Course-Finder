{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "attach",
        "name": "Node: Nodemon",
        "processId": "${command:PickProcess}",
        "port": 8080,
        "restart": true,
        "protocol": "inspector",
        "localRoot": "${workspaceFolder}/src",
      }
    ]
  }